#include <stdio.h>

int main () {
    int x= 0, y=1, z=-1;

    //exceto o 0 que significa false
    //entre numeros inteiros qualquer valor ativa o if
    if (x)
        printf("certo");

    return 0;
}